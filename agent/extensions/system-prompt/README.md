# system-prompt

`system-prompt` adds a `/system-prompt` command that appends the current constructed system prompt to the session transcript.

The transcript entry includes the model, working directory, timestamp, and the prompt in a fenced code block.
